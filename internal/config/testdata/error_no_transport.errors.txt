error: No transport enabled — Declare at least one of `wireguard { ... }` or `tailscale { ... }` inside the `gateway { ... }` block. Block presence selects the transport; both may be enabled simultaneously.
